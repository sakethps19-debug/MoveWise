# Copy to .env.local. Points at the same SQLite file packages/db uses —
# relative to apps/web's own cwd (see docs/architecture.md for why this
# needs a different relative path than packages/db/.env.example does).
DATABASE_URL="file:../../packages/db/dev.db"
