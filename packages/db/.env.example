# Copy to .env. Used by the Prisma CLI (db:generate / db:push) — the app
# itself reads DATABASE_URL from apps/web/.env.local at runtime, not this
# file. Swap for a real Postgres connection string when ADR-0002's
# SQLite-to-Postgres migration happens.
DATABASE_URL="file:./dev.db"
